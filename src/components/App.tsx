import { Editor } from "./Editor";
import { MainMenu } from "./MainMenu";
import { useState } from "react";

export const App = () => {
    return (
        <div className="App">
            <MainMenu />
        </div>
    )
};
